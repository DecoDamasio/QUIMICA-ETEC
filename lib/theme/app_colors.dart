import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF2387FF);
  static const secondary = Color.fromARGB(255, 255, 255, 255);
  static const tertiary = Color.fromARGB(255, 209, 222, 224);
  static const background = Color.fromARGB(255, 206, 224, 226);

  static const textPrimary = Colors.black;
  static const textSecondary = Colors.grey;

  //cores da pagina de menu
  static const success = Color(0xFF4ADE80); 
  static const info = Color(0xFF3B82F6);    
  static const cardBg = Colors.white;
}